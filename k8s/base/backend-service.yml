apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: quiz-platform
spec:
  selector:
    app: quiz-backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
