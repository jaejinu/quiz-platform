# GitHub OAuth App에서 발급 (docs/oauth-setup.md 참조)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# 프론트엔드 콜백 URL (local 기본값 사용 시 생략 가능)
QUIZ_AUTH_POST_LOGIN_REDIRECT=http://localhost:5173/auth/callback

# JWT secret (Step 7, HS256 32 bytes 이상)
QUIZ_JWT_SECRET=dev-local-secret-min-32-bytes-xxxxxxxxxx
